; supply-chain hardening
ignore-scripts=true
; fail install if node/npm version doesn't satisfy engines
engine-strict=true
; refuse to resolve any package version published < 3 days ago (value is in DAYS).
min-release-age=3
fund=false
